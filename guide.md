# create a app for download youtube videos

`tech stack use`
    - frontend: tailwindcss
    - backend: nodejs + expressjs

`workflow`

1. create the files of node + express js that for download yt vidoes
2. then create the frontend using tailwind css 

`essentials for frontend`
1. use: goole's Caveat font family
2. feature would need to be as when user paste the youtube link then need to will be able to choose download format (360p 480p either 720p... like wise)
3. other thing is user paste the url then need to shows video's thubnail on small image, video's title, video's description
4. after showing all the details user choose the download format then download should be start and shows save to which path of the pc.either whatelse
5. after downloaded the  video then need to shows successful message as download successfully..like wise
6. other thing is better to shows carosoul on the frontend 
7. color palate -  use light themed color palate like shows other sites they shows like pirate download sites, but instead of pirate color make it premium and user friendly
8. use smooth animations and transitions



`essentials for backend`
1. need to make feature theirfor youtube video donwload (below mentioned download format)

# no database need for this so just use backend and frontend so that case's user's pc / eiher device act as storage

`workflow`
1. first setup backend and then notify it to get confirmation to proceed to dev the front end
2. then create frontend and notify it also.
3. after that overall finished the development notify it