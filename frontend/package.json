{
  "name": "ythub-frontend",
  "version": "1.0.0",
  "description": "YouTube video downloader frontend",
  "scripts": {
    "start": "npx live-server --port=3000 --open=index.html",
    "dev": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}